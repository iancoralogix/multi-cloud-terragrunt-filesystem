locals {
  # This is globally unique across all of GCP
  project = "project-o11y"
  # This is globally unique across all of GCP, duplicated for convenience.
  project_id = "project-o11y"
  # internal name of the project, and the directory it lives in
  project_name = "project-o11y"
  region       = "us-central1"
}
